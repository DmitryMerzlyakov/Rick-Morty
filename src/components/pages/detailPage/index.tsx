import { DetailInfo } from '@/components/dummies';
import { PageWrapper } from '@/components/wrapper';

export const DetailPage = () => {

  return (
    <PageWrapper>
      <DetailInfo />
    </PageWrapper>
  );
};
